<tr class="border border-primary">
  <td><span class=""><%= test.id %></span></td>
  <td><%= test.title %></td>
  <td><%= test.level %></td>
  <td><%= test.questions.count %></td>

  <td>
    <%= button_to t('.start'), start_test_path(test), class: 'btn btn-primary' %>
  </td>
</tr>
